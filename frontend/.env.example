PORT=3000

# Base de datos — usar IP privada real de EC2 BD
DB_HOST=10.0.1.X        # reemplaza X con la IP real cuando la tengas
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=SimiERP2026!  # debe coincidir exactamente con docker-compose
DB_NAME=simi_erp          # debe coincidir exactamente con docker-compose

# JWT — usar una clave larga y aleatoria
JWT_SECRET=simi_jwt_2026_clave_ultra_secreta_farmacias_!#$

# URL frontend (para CORS en producción)
FRONTEND_URL=http://DNS-DEL-ALB-AQUI