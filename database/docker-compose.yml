version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: postgres-simi
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: SimiERP2026!
      POSTGRES_DB: simi_erp
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql